#! /bin/sh
# /etc/init.d/blah
#

# Some things that run always

chdir /home/pi/tingo

# Carry out specific functions when asked to by the system
case "$1" in
  start)
    echo "Starting Tingo..."
    sudo python tingo.py & >/dev/null
    ;;
  stop)
    echo "Stopping Tingo..."
    sudo pkill -1 -f tingo.py
    ;;
  restart)
    echo "Restarting Tingo..."
    sudo tingo stop >/dev/null
    sudo tingo start >/dev/null
    ;;
  update)
    echo "Updating Tingo..."
    git pull
    tingo restart >/dev/null
    ;;
  *)
    echo "Usage: sudo tingo {start|stop|restart|update}"
    exit 1
    ;;
esac

exit 0

